using System.Data.SQLite;

namespace homeworld
{
    public class Program
    {
        public static void Main()
        {
           /*string connectionString = @"URI=file:../game.db";

            using var connection = new SQLiteConnection(connectionString);
            connection.Open();

            string addStatement = "INSERT INTO all_entities(human_label) VALUES('jod')";
            using var command2 = new SQLiteCommand(addStatement, connection);
            command2.ExecuteNonQuery();

            string statement = "SELECT * FROM all_entities";
            using var command = new SQLiteCommand(statement, connection);
            using SQLiteDataReader reader = command.ExecuteReader();*/
        }
    }
}